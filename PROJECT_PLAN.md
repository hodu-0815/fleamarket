# 우리동네 플리마켓 — 프로젝트 플랜

이 문서는 기능 범위, 일정, 데이터 모델, 코드 분리 설계를 한 곳에 모아둔 참고 문서입니다. 협업 방법론(브랜치/리뷰/프롬프트 가이드)은 [AGENTS.md](AGENTS.md)를 참고하세요.

## 0. 전제

- 남은 기간: 1~2주, 두 사람 모두 평일 퇴근 후 짬시간 작업 (가용 시간이 넉넉하지 않음)
- 한임은 개발 경험이 있고, 유진은 코드 이해가 없는 디자이너로 화면 단위를 AI에게 직접 프롬프트로 구현
- **작업 분담 (확정)**: 회원가입 / 로그인 / 마이페이지 = 한임, 상품(등록·수정·삭제) + 목록(메인피드) = 유진. 상세 파일 단위 분담은 [AGENTS.md](AGENTS.md) 8~9절 참고
- 결정이 필요했던 2가지 항목은 아래와 같이 잠정 확정하고 진행합니다. 다르게 하고 싶으면 언제든 이 문서를 수정해서 알려주세요.
  - **Phase 구분**: 아래 1절의 Phase 1/2/3 구성으로 확정하고 시작합니다. 친구맵은 Phase 3(여유 있을 때)로 미룹니다.
  - **AI 도구**: 한임은 Cursor, 유진은 Codex를 씁니다. 둘 다 `AGENTS.md`를 공통으로 읽으므로 스택 규칙과 협업 규칙은 `AGENTS.md`에 둡니다. `CLAUDE.md`는 `AGENTS.md`를 가리키는 짧은 안내문만 남겨서, 혹시 Claude Code로 작업하는 경우에도 같은 규칙을 보게 합니다.

## 1. 기능 범위 (Phase)

### Phase 1 — 필수, 1주차 목표 (이게 없으면 서비스 자체가 안 됨)

- 회원가입: ID, PW, 초대코드, 닉네임 (필수 인풋만)
- 로그인: ID/PW, 자동로그인
- 메인피드: 목록(썸네일/타이틀/가격/판매자 닉네임/찜 개수) + 카드 클릭 시 상세 팝업(설명, 액션 포함). 상세컷은 1장으로 축소해서 시작
- 상품 등록/수정/삭제: 썸네일, 타이틀, 가격, 설명, 카테고리
- 찜 토글
- 마이페이지 최소 버전: 내 찜목록, 내가 올린 상품목록, 로그아웃

### Phase 2 — 2주차 목표 (여유 있으면)

- 프로필 확장 필드: 유진(주인장)과의 관계, 자기소개 한줄, 방문시간, 저녁먹을지, 프로필이미지(직접 업로드 / 미등록시 랜덤 배치)
- 마이페이지 내정보수정
- 상세컷 이미지 3장 전체 지원

### Phase 3 — 시간 남으면 / 런칭 후 추가

- 친구맵 페이지(타임라인 뷰): 친구 카드(닉네임, 유진과의 관계, 올린 상품 목록, 방문시간 범위)

## 2. 일정 로드맵 (평일 짬시간 기준, 대략적인 가이드)

- 1~2일차: Supabase 스키마 설계 + 초대코드 회원가입/로그인/자동로그인 (한임 담당)
- 3~5일차: 메인피드 + 상세팝업 + 찜 (유진이 화면 UI를 AI로 구현, 한임은 데이터 연동부 리뷰)
- 5~7일차: 상품 등록/수정/삭제 폼(카테고리 포함) (유진 담당, 한임 리뷰)
- 7~9일차: 마이페이지 최소 버전(한임 담당, 유진 디자인 리뷰) + Phase 1 통합 테스트(모바일 320~430px 기준 — PC 버전은 따로 만들지 않음)
- 9~11일차 (2주차 여유분): 프로필 확장 필드(마이페이지, 한임), 상세컷 3장(상품, 유진), 친구맵 착수(한임)

일정이 밀리면 Phase 2/3부터 순서대로 다음 스프린트로 미루고, Phase 1은 반드시 지키는 것을 원칙으로 합니다.

## 3. 데이터 모델 (Supabase 스키마 초안)

실제 컬럼 타입/제약조건은 구현 시점에 다시 다듬어야 하는 "초안"입니다.

- `profiles` — 사용자 프로필
  - `id` (auth user id), `nickname`, `password`(또는 Supabase Auth 사용시 불필요), `is_admin`
  - Phase 2: `relationship`(유진과의 관계, text), `bio`(자기소개 한줄), `visit_time`, `dinner`(boolean/enum), `avatar_url`
- `invite_codes` — 초대코드 관리
  - `code`, `issued_at`, `used_by`(nullable, 사용된 profile id), `used_at`
- `products` — 상품
  - `id`, `owner_id`(profiles 참조), `title`, `price`, `description`, `category`(옷/전자기기/인테리어/잡화/화장품/레저/주방용품/재능 중 하나), `thumbnail_url`, `created_at`
- `product_images` — 상세컷 (Phase 2, 최대 3장)
  - `id`, `product_id`, `url`, `sort_order`
- `likes` — 찜
  - `product_id`, `user_id`, `created_at` (복합 unique key로 중복 찜 방지)

카테고리 값은 코드에서 상수 배열로 관리하고(`CATEGORIES`), DB에는 문자열로 저장하는 방식을 제안합니다 (별도 카테고리 테이블 없이 시작, 필요해지면 나중에 정규화).

## 4. JS 모듈 분리 설계

현재 [app.js](app.js) 하나에 모든 로직이 들어있는데, 기능이 크게 늘고 두 사람이 동시에 작업하므로 화면 단위로 파일을 쪼갭니다. 번들러 없이 브라우저 네이티브 ES 모듈(`<script type="module">`)만 사용합니다.

```
index.html
styles.css
app.js              (엔트리포인트: 각 모듈 init() 호출, 탭 전환만 담당)
store.js            (state, loadState/saveState, localStorage) — 한임 전용
supabase-client.js  (setupSupabase, 공용 helper) — 한임 전용
auth.js             (회원가입/로그인/초대코드/자동로그인) — 한임 담당
mypage.js           (마이페이지) — 한임 담당
feed.js             (메인피드 목록 + 상세팝업 + 찜) — 유진 담당
product-form.js     (상품 등록/수정) — 유진 담당
friendmap.js        (친구맵 타임라인, Phase 3) — 한임 담당
utils.js            (escapeHtml, formatPrice, formatDate 등 공용 함수) — 한임 전용
```

- 각 모듈은 `export function init() { ... }` 형태로 자신의 DOM 바인딩/이벤트 리스너를 등록
- `app.js`는 `import { init as initAuth } from "./auth.js"` 식으로 모듈들을 모아서 초기화 순서만 조율
- 공용 상태(`store.js`)와 공용 유틸(`utils.js`)은 한임만 수정하고, 다른 모듈은 이 둘을 `import`해서 사용
- 실제 파일 분리 작업(코드 이동)은 별도 구현 단계에서 진행 (이 문서는 설계까지만 다룸)
