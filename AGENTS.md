# AGENTS.md — 프로젝트 규칙 & 협업 가이드

이 문서는 AI 코딩 도구(Codex, Cursor, Claude Code 등 무엇을 쓰든 동일)로 이 프로젝트를 작업할 때 지켜야 하는 모든 규칙을 담은 단일 문서입니다. `AGENTS.md`는 Codex/Cursor/Claude Code 등 대부분의 AI 코딩 도구가 공통으로 읽는 표준 파일명이라 여기를 기준 문서로 둡니다. (`CLAUDE.md`는 이 문서를 가리키는 안내만 담고 있습니다.)

기능 범위/일정/데이터 모델은 [PROJECT_PLAN.md](PROJECT_PLAN.md)를 참고하세요.

## 1. 프로젝트 개요 및 스택 규칙

- 이 프로젝트는 정적 vanilla 웹앱입니다. 프레임워크를 쓰지 않습니다.
- 클라이언트 언어는 HTML, CSS, JavaScript입니다.
- 프레임워크는 사용하지 않습니다. 사용자가 명시적으로 요청하지 않는 이상 React, Vue, Svelte, Tailwind, 빌드 도구, 패키지 매니저, 번들러를 도입하지 않습니다.
- 실행 파일은 프로젝트 루트에 있습니다.
  - 마크업/DOM 템플릿: `index.html`
  - 스타일/디자인 토큰: `styles.css`
  - 상태, 렌더링, 이벤트 로직: `app.js` (화면이 늘어나면서 화면별 모듈로 분리 중 — 8절 참고)
- 실행은 `index.html`을 브라우저로 열거나 `python3 -m http.server`로 확인합니다. `npm install` 같은 게 필요하다고 AI가 제안하면 잘못된 방향입니다 — 진행하지 말고 확인하세요.

## 2. Figma 작업 플로우 (Figma MCP로 디자인을 반영할 때)

1. 정확한 Figma 노드에 대해 `get_design_context`를 먼저 실행합니다.
2. 응답이 너무 크거나 잘리면 `get_metadata`로 노드 맵을 확인한 뒤, 필요한 노드만 `get_design_context`로 다시 가져옵니다.
3. 구현 전에 동일한 노드/variant에 대해 `get_screenshot`을 실행합니다.
4. 디자인 컨텍스트와 스크린샷을 모두 확인한 뒤에만 Figma 에셋을 다운로드하거나 참조합니다.
5. Figma MCP 결과물은 "디자인 의도"로만 취급하고 최종 코드 스타일로 취급하지 않습니다. React/Tailwind 예시 코드가 나오면 이 프로젝트의 vanilla HTML/CSS/JS 컨벤션으로 변환합니다.
6. 구현이 끝나면 Figma 스크린샷과 비교해서 시각적 일치, 반응형(모바일 기준) 동작, 인터랙션 상태를 확인한 뒤 완료 처리합니다.

## 3. 컴포넌트/마크업 규칙

- 새 구조를 추가하기 전에 `index.html`의 기존 시멘틱 섹션, form, button, `#productTemplate` 패턴을 먼저 재사용합니다.
- JavaScript로 렌더링되는 반복 마크업은 `<template>`에 배치합니다.
- 시멘틱 HTML을 우선 사용합니다: `header`, `nav`, `main`, `section`, `article`, `form`, `button`, `label`, `input`, `textarea`.
- 디자인이 명시적으로 바꾸지 않는 한 한글 사용자 문구를 유지합니다.
- 기존 패턴에 맞춰 접근성 속성을 유지합니다: `aria-label`, `aria-labelledby`, 아이콘 전용 버튼의 `title`, `label for`와 `input id` 짝, 의미 있는 이미지 `alt` 텍스트.
- Figma 데이터에 실제 텍스트/이미지/localhost 에셋 URL이 있으면 placeholder UI를 쓰지 않습니다.

## 4. 스타일링 규칙

- `styles.css`에 순수 CSS만 사용합니다. Tailwind, CSS-in-JS, Sass, CSS Modules, inline style 속성을 추가하지 않습니다.
- 디자인 토큰은 `styles.css`의 `:root`에 있는 CSS 커스텀 속성입니다.
- 새 값을 하드코딩하기 전에 기존 CSS 변수를 재사용합니다: `--bg`, `--surface`, `--paper`, `--ink`, `--muted`, `--line`, `--brand`, `--brand-dark`, `--green`, `--blue`, `--heart`, `--shadow`.
- 디자인 시스템에 속하는 반복되는 색상/그림자/간격 값은 `:root`에 새 토큰으로 추가하고, 셀렉터마다 하드코딩 값을 흩어놓지 않습니다.
- 모바일 앱쉘 패턴을 유지합니다: `width: min(100%, 430px)`, `min-width: 320px`. 이 프로젝트는 PC 버전을 따로 만들지 않으므로, 넓은 화면 대응은 기존 쉘이 안 깨지는 정도(중앙 정렬 등)만 유지하면 됩니다.
- Figma 디자인이 명확히 다른 시스템을 제시하지 않는 한, 기존 8px 라운드 컨트롤/카드 언어를 유지합니다.
- 탭 버튼, 아이콘 버튼, 상품 카드, 이미지 타일, 폼 컨트롤 같은 고정 형식 UI는 안정적인 크기를 유지해서 동적 텍스트나 hover/focus 상태로 레이아웃이 흔들리지 않게 합니다.
- Figma 텍스트 스타일이 명확히 요구하지 않는 한 `letter-spacing: 0`을 유지합니다.
- 특히 한글과 사용자 생성 상품 콘텐츠에서 기존 `overflow-wrap: anywhere` 동작으로 텍스트가 안전하게 줄바꿈되도록 합니다.

## 5. JavaScript 규칙

- vanilla JavaScript만 사용하고 의존성을 추가하지 않습니다.
- 앱 상태는 `loadState()`로 불러오고 `saveState()`로 저장하는 중앙화된 `state` 객체에 유지합니다.
- 클라이언트 저장은 기존 `STORAGE_KEY` 패턴의 `localStorage`를 사용합니다.
- UI는 `renderSession()`, `renderNotice()`, `renderProducts()`, `render()` 같은 명시적 render 함수를 통해 그립니다.
- 사용자 생성 HTML은 `innerHTML`에 넣기 전에 `escapeHtml()`로 escape하고, 가능하면 동적 텍스트에는 `textContent`를 우선 사용합니다.
- 이벤트 리스너는 HTML에 inline으로 넣지 않고 JS 파일에서 등록합니다.
- 기능별 헬퍼 함수는 작게 쪼개고, 기존 camelCase 네이밍 규칙을 따라 동작 기준으로 이름 짓습니다.

## 6. 에셋 처리

- Figma MCP 서버는 localhost URL로 이미지/SVG 에셋을 제공할 수 있습니다.
- Figma MCP가 localhost 소스를 반환하면 그대로 사용하거나, 영구 저장이 필요한 경우에만 프로젝트에 다운로드합니다.
- 아이콘 패키지를 설치하거나 import하지 않습니다. Figma 에셋이나 기존의 단순 텍스트/아이콘 버튼 패턴을 사용합니다.
- 영구 에셋이 필요하면 루트에 `assets/` 디렉토리를 만들고 `index.html`이나 `styles.css`에서 상대 경로로 참조합니다.
- Figma 데이터에 실제 에셋이 있는데 제네릭 placeholder 이미지를 새로 만들지 않습니다.

## 7. 품질 체크

- Figma 기반 UI 변경 후에는 정적 페이지로 실행(`python3 -m http.server` 등)해서 영향받은 뷰포트 너비를 확인합니다.
- 모바일 너비(320px~430px)를 기준으로 확인합니다. PC 버전은 따로 만들지 않으므로 데스크톱 확인은 생략해도 됩니다.
- 컨트롤이 키보드로 접근 가능한지, focus 상태가 보이는지, 상품/사용자 생성 텍스트가 넘치지 않는지, 모든 폼 label이 input과 연결돼 있는지 확인합니다.
- 이 프로젝트에는 테스트 프레임워크가 없으므로, 코드를 변경했으면 최종 응답에 수동 확인 방법을 남깁니다.

## 8. 파일별 작업 경계

기능이 커지면서 [app.js](app.js) 하나에 다 몰아넣지 않고, 화면 단위 파일로 나눕니다(설계는 [PROJECT_PLAN.md](PROJECT_PLAN.md) 4절 참고). **자기 담당 파일만 수정**하는 것이 원칙입니다. 같은 파일을 동시에 건드리면 vibe coding 특성상 git 충돌이나 서로 작업을 덮어쓰는 사고가 잘 납니다.

| 파일 | 담당 | 설명 |
| --- | --- | --- |
| `store.js`, `supabase-client.js`, `utils.js` | 한임 | 공용 상태/DB 연결/공용 함수. 다른 파일은 여기서 `import`만 해서 씀 |
| `auth.js` | 한임 | 회원가입, 로그인, 초대코드, 자동로그인 |
| `mypage.js` | 한임 | 마이페이지 (찜목록, 내 상품, 내정보수정, 로그아웃) |
| `feed.js` | 유진 | 메인피드 목록, 상세 팝업, 찜 |
| `product-form.js` | 유진 | 상품 등록/수정 폼 |
| `friendmap.js` | 한임 | 친구맵 타임라인 (Phase 3) |
| `index.html`, `styles.css`의 전역 구조/토큰 | 한임 | 새 `<template>`이나 `:root` 토큰 추가는 한임과 상의 후 |

담당 기준: 회원가입/로그인/마이페이지 = 한임, 상품과 목록(메인피드) 관련 화면 = 유진.

공용 파일(`store.js`, `supabase-client.js`, `utils.js`)은 한임이 관리합니다. 유진이 수정이 필요할 것 같으면, 직접 고치지 말고 먼저 한임에게 요청하세요. 여기가 꼬이면 전체 화면이 다 같이 깨집니다.

## 9. 작업 분담표

| 영역 | 한임 (개발) | 유진 (디자인/vibe coding) |
| --- | --- | --- |
| 데이터 모델 / Supabase 스키마 설계 | O | - |
| 회원가입 / 로그인 / 초대코드 검증 | O | - |
| 마이페이지 (찜목록, 내 상품, 내정보수정, 로그아웃) | O | 리뷰만 |
| API 연동, 인증 관련 보안 로직 | O | - |
| 메인피드 카드 / 상세 팝업 UI | 리뷰만 | O |
| 상품 등록·수정 폼 UI | 리뷰만 | O |
| 친구맵 타임라인 UI (Phase 3) | O | 리뷰만 |
| 모바일 화면 QA (PC 버전은 따로 안 만듦) | 같이 | 같이 |

유진이 만든 화면(메인피드/상품폼)은 항상 화면 자체(UI)에 집중하고, 데이터를 어떻게 저장/조회하는지(Supabase 쿼리, 인증 체크)는 한임이 만든 공용 함수를 그대로 호출하는 선에서 작업합니다. 반대로 마이페이지/친구맵은 한임이 만들지만, 화면 디자인/레이아웃이 기존 디자인 시스템과 맞는지는 유진이 리뷰합니다.

## 10. Git / 리뷰 워크플로우

1. 작업 시작 전 `git pull`로 최신 상태를 받습니다.
2. 화면 단위 브랜치를 만듭니다. 예: `feature/feed`, `feature/product-form`, `feature/mypage`.
3. 브라우저에서 직접 동작을 확인한 뒤에는 바로 `main`에 merge해도 됩니다. 별도 승인 절차 없이 각자 merge 가능합니다. 다만 공용 파일(`store.js`, `supabase-client.js`, `utils.js`)을 건드렸거나 다른 화면에 영향을 줄 수 있는 변경이면 merge 전에 한임에게 먼저 알려주세요.
4. 커밋은 작은 단위로 나눠서 합니다. AI가 만든 diff를 검토 없이 통째로 커밋하지 않습니다 — 실제로 브라우저에서 동작을 확인한 뒤 커밋하세요.

## 11. 유진용 리뷰 체크리스트 (merge 전 스스로 체크)

- [ ] 브라우저 콘솔(F12)에 에러가 없다
- [ ] 모바일 너비(375px 근처)에서 레이아웃이 깨지지 않는다 (PC 버전은 따로 만들지 않으므로 확인 안 해도 됨)
- [ ] 새로운 색상/그림자를 하드코딩하지 않고 기존 `:root` 변수를 썼다
- [ ] 폼에 `label`과 `input id`가 짝이 맞는다 (접근성)
- [ ] 사용자가 입력한 텍스트가 화면에 그대로 꽂히는 부분은 `escapeHtml()`을 거친다
- [ ] 내가 맡은 파일(`feed.js`, `product-form.js`) 외의 다른 파일을 건드리지 않았다 (건드렸다면 왜 그랬는지 한임에게 공유)

## 12. 프롬프트 작성 가이드 (유진이 AI에게 요청할 때)

범위를 좁게 지정할수록 AI가 엉뚱한 파일을 건드릴 위험이 줄어듭니다.

**좋은 예**
- "`feed.js` 파일만 수정해서 메인피드 카드에 찜 개수를 표시해줘. 다른 파일은 건드리지 마."
- "이 화면은 `AGENTS.md`에 있는 기존 패턴(state/render 함수, `escapeHtml`)을 그대로 따라서 만들어줘."
- "새 CSS는 만들지 말고 `styles.css`의 기존 `:root` 변수만 써줘."

**피해야 할 예**
- "플리마켓 앱에 상품 목록/등록 화면 다 만들어줘" — 범위가 넓어서 AI가 `feed.js`, `product-form.js`를 동시에 건드리며 예상 못한 곳까지 바꿀 수 있습니다. 화면 하나씩 나눠서 요청하세요.
- "그냥 알아서 예쁘게 만들어줘" — 디자인 시스템(색상 토큰, 카드 규격)을 안 따를 위험이 있습니다.

## 13. 막혔을 때

- AI가 이해되지 않는 에러를 계속 낼 경우, 직접 코드를 이것저것 고치지 말고 **에러 메시지를 그대로 캡처**해서 한임에게 공유하세요.
- 새 테이블, 새 파일 추가, 공용 함수 시그니처 변경처럼 다른 화면에 영향을 줄 수 있는 변경은 항상 사전에 상의하고 진행합니다.
- Phase 1 일정이 밀릴 것 같으면 참지 말고 바로 얘기해서 [PROJECT_PLAN.md](PROJECT_PLAN.md)의 범위를 같이 조정합니다.
